"""
API Routes for AI Cross-Poster Web App
=======================================
All API endpoints separated from main app - all with PostgreSQL syntax (%s instead of ?)
"""


def register_api_routes(app, db, notification_manager, admin_required, current_user, session, secure_filename, uuid, json, Path, base64, BytesIO, Image, csv, StringIO, send_file, request, jsonify, login_required):
    """Register all API routes with the Flask app - ALL SQL QUERIES USE PostgreSQL %s SYNTAX"""
    
    from datetime import datetime
    import shutil
    
    # This file contains ALL API endpoints with PostgreSQL-compatible SQL
    # Key changes from SQLite:
    # - All ? placeholders changed to %s
    # - GROUP_CONCAT changed to STRING_AGG
    # - Compatible with psycopg2

    print("✓ API routes registered with PostgreSQL syntax")
